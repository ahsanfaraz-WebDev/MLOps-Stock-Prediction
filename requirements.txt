# Core ML Libraries (Python 3.8 compatible)
pandas>=2.0.0,<2.2.0
numpy>=1.24.0,<1.27.0
scikit-learn>=1.3.0

# API and Web Framework
requests>=2.31.0
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0,<2.6.0

# MLOps Tools
# Note: apache-airflow is already installed in base image
mlflow>=2.8.0
# DVC with AWS S3 support (latest available version for Python 3.8)
# Note: DVC 3.42.0 is the latest version available
dvc[s3]>=3.42.0
# AWS SDK for Python (boto3) - required for S3 access
boto3>=1.28.0

# Monitoring
prometheus-client>=0.17.0

# Data Quality & Profiling
# Note: pandas-profiling is deprecated, using ydata-profiling instead
ydata-profiling>=4.0.0
great-expectations>=0.18.0

# Utilities
python-dotenv>=1.0.0
pytest>=7.4.0
pytest-cov>=4.1.0
flake8>=6.0.0

# Joblib for model saving
joblib>=1.3.0

# CI/CD Tools
# Note: CML is installed separately in GitHub Actions workflows
# cml is installed via: pip install dvc[all] cml (in workflow)
codecov>=2.1.13